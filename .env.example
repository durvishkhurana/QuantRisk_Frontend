# Backend API origin (no trailing slash). Required for production builds.
# This is the only variable the frontend needs — auth is JWT against the backend.
VITE_API_URL=https://your-backend.onrender.com
